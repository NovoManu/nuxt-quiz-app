export const state = () => ({})
export const actions = {
  async nuxtServerInit({ dispatch, rootGetters }) {
    // if (rootGetters['auth/authorized']) {
    //   await dispatch('notifications/fetchServerNotifications', null, {
    //     root: true
    //   })
    // }
  }
}
